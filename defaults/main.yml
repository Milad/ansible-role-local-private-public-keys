---

# The user who will have the public key added to their authorized_key.
controller_enabled_user: ""

# A string of ssh key options to be prepended to the key in the authorized_keys file.
authorized_key_key_options: 'no-port-forwarding,no-agent-forwarding,no-X11-forwarding,command="~/ansible/ansible-remote.py"'
